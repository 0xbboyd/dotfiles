# ~/.config/tmuxinator/universal.yml

name: <%= File.basename(Dir.pwd).gsub('.', '_') %>
root: <%= Dir.pwd %>

windows:
  - <%= File.basename(Dir.pwd) %>:
      layout: even-horizontal
      panes:
        - hermes sessions browse
        - yazi

# ~/.config/tmuxinator/universal.yml

name: <%= File.basename(Dir.pwd).gsub('.', '_') %>
root: <%= Dir.pwd %>

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim .
        - 

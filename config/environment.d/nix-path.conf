# Add nix profiles to PATH for graphical sessions (sway, i3, etc.)
# Shell init (.zshrc) handles interactive shells; this covers non-shell processes
# launched by the display manager.
PATH=/nix/var/nix/profiles/default/bin:/home/bboyd/.nix-profile/bin:${PATH}
